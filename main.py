# from producer.producer import producer
# from data_processing.data_consumer import data_consumer
# from authentication.reddit_OAuth2 import *
import os

if __name__ == '__main__':
    # user_agent = "script:producer_reddit_api:v1.0 (by /u/ujzwalp1710)"
    # redirect_url = "http://localhost:8080"
    # client_id = os.environ.get("client_id")
    # client_secret = os.environ.get("client_secret")
    
    # OAuth_obj = OAuthRedditAccess(client_id, client_secret, user_agent, redirect_url)
    # OAuth_obj.OAuth2()
    # producer()
    # data_consumer()
    